<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--约束文件 要出出现的标签符合mybatis要求-->
<!--namespace 要写dao接口的路径-->
<mapper namespace="com.rt.dao.ReaderTypeDao">
    <select id="selectAllReaderType" resultType="com.rt.pojo.ReaderType">
        select id as rType, reader_type as rTypeDetail from class
    </select>

</mapper>